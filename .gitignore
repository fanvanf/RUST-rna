# Screenshots & test artifacts
*.png
*.jpeg
*.gif
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Rust (for future backend)
/target/
Cargo.lock

# Node (if build tools added later)
node_modules/
dist/

# Bundled sidecar binaries populated by CI before `cargo tauri build`.
# Kept empty in the repo — local `cargo tauri dev` falls back to $PATH.
crates/rb-app/binaries/*
!crates/rb-app/binaries/.gitkeep

# Tauri build output: schemas regenerated by tauri-build on every cargo run.
crates/rb-app/gen/

.codex
